# Объём работ под наш проект (без смены стека)

**Хостинг:** https://play2go.cloud (размещение на этом домене; вход через `/auth?to=/me` при необходимости).  
**Стек не меняем:** React + Vite + Express + SQLite + Google Gemini.

Полное ТЗ: [TZ-FULL-2026.md](TZ-FULL-2026.md). Соответствие текущего кода ТЗ: [TZ-COMPLIANCE.md](TZ-COMPLIANCE.md).

---

## Что берём из ТЗ под наш проект

| Из ТЗ | Решение по проекту |
|-------|---------------------|
| Адаптив, тема, счётчики, SRS, квизы, AI, словарь, поиск, экспорт | Уже есть — не трогаем. |
| Хостинг / домен | Размещение на **play2go.cloud** (вместо Vercel или вместе с ним — как решите). |
| Деплой, документация | README и [DEPLOYMENT.md](DEPLOYMENT.md) обновить под play2go.cloud. |
| Безопасность (HTTPS, CORS, ключи в env) | Уже есть; при необходимости добавить rate-limit. |
| Доступность (focus, aria) | По мере доработок сохранять и улучшать. |
| PWA / офлайн | Оставить как есть (vite-plugin-pwa), при необходимости дополировать manifest. |

---

## Что из ТЗ не делаем (выделено под наш проект)

| В ТЗ | Решение |
|------|---------|
| Next.js, SSR/SSG, Vercel Functions | **Не переходим** — остаёмся на Vite + Express. |
| PostgreSQL / MongoDB / Supabase | **Не переходим** — остаёмся на SQLite. |
| OpenAI | **Не переходим** — остаёмся на Gemini. |
| Аутентификация (логин, OAuth, users) | **Не в объёме** — без своей системы логина; если нужен вход — через play2go.cloud (например `/auth?to=/me`). |
| Локализация i18n (RU/EN) | **Не в приоритете** — интерфейс русский. |
| Избранные / Архив в словаре | **Не в объёме** — только «Все» + поиск и фильтры. |
| Экспорт в PDF | **Не в объёме** — достаточно CSV/JSON. |
| Chart.js, графики прогресса | **Не в объёме** — цель дня и streak достаточно. |
| Unit/Integration/E2E тесты, CI/CD | **Не в приоритете** — по желанию позже. |
| Swagger, Sentry, load-тесты | **Не в объёме**. |
| MGIMO API, LMS | **Не в объёме** — только текущий функционал. |

---

## Итог

- **Стек:** React, Vite, Express, SQLite, Gemini — без изменений.  
- **Размещение:** play2go.cloud (URL и при необходимости вход через `/auth?to=/me`).  
- **Реализуем из ТЗ:** только то, что перечислено в таблице «Что берём»; остальное из ТЗ считаем не входящим в текущий объём работ.
